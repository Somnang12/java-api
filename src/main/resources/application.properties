spring.application.name=web-api-product

spring.servlet.multipart.max-file-size=30MB
spring.servlet.multipart.max-request-size=100MB

# --- Database Config (PostgreSQL) ---
# JDBC URL format for your specific Render database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d5mrsjh4tr6s73cs4340-a.virginia-postgres.render.com:5432/somnang}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:somnang}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ccIWchxTsQmLrGLGLSXplRyx0dlqqcRM}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Connection Pool (Hikari) ---
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true

# --- JPA / Hibernate ---
# Tells Hibernate to use PostgreSQL syntax
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true 
spring.jpa.open-in-view=true

spring.web.error.include-message=always